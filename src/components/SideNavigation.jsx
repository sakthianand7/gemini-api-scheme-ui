import {
    SideNavigation
} from '@cloudscape-design/components';

export default () => {
    return (<SideNavigation
        header={{
            href: '#',
            text: 'Service name',
        }}
    />);
}